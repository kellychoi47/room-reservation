import { useEffect,useState } from 'react';import { api } from '../lib/supabase';import Spinner from '../components/Spinner';import Status from '../components/Status';
export default function Admin(){const [data,setData]=useState();const [err,setErr]=useState('');const load=()=>api('/admin/overview').then(setData).catch(e=>setErr(e.message));useEffect(load,[]);const cancel=async id=>{if(!confirm('Cancel this reservation?'))return;try{await api(`/reservations/${id}/cancel`,{method:'POST'});load()}catch(e){setErr(e.message)}};if(err)return <div className="alert error">{err}</div>;if(!data)return <Spinner/>;return <><div className="page-title"><div><p className="eyebrow">ADMINISTRATION</p><h2>Workspace control center</h2><p>Manage rooms and monitor upcoming usage.</p></div></div><div className="stats">{[['Active rooms',data.stats.rooms],['Upcoming bookings',data.stats.reservations],['Users',data.stats.users]].map(([l,n])=><div className="stat" key={l}><span>{l}</span><strong>{n}</strong></div>)}</div><section className="card"><div className="section-head"><div><h3>Upcoming reservations</h3><p>All reservations for the next 30 days</p></div></div><div className="table-wrap"><table><thead><tr><th>Reservation</th><th>Room</th><th>Reserved by</th><th>When</th><th>Status</th><th></th></tr></thead><tbody>{data.reservations.map(r=><tr key={r.id}><td><b>{r.title}</b></td><td>{r.rooms.name}</td><td>{r.profiles.full_name}</td><td>{new Date(r.start_time).toLocaleString('en',{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'})}</td><td><Status value={r.status}/></td><td>{r.status==='confirmed'&&<button className="text-danger" onClick={()=>cancel(r.id)}>Cancel</button>}</td></tr>)}</tbody></table></div></section></>}
