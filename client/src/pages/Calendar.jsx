import { useEffect,useState } from 'react';import { api } from '../lib/supabase';import Spinner from '../components/Spinner';
export default function Calendar(){const [data,setData]=useState();const [start,setStart]=useState(new Date().toISOString().slice(0,10));useEffect(()=>api(`/reservations?start=${start}`).then(setData),[start]);if(!data)return <Spinner/>;const days=Array.from({length:7},(_,i)=>{const d=new Date(start+'T12:00:00');d.setDate(d.getDate()+i);return d});return <><div className="page-title calendar-title"><div><p className="eyebrow">SCHEDULE</p><h2>Reservation calendar</h2><p>See reserved times across all available rooms.</p></div><input type="date" value={start} onChange={e=>setStart(e.target.value)}/></div><div className="calendar card"><div className="calendar-head">{days.map(d=><div key={d}><small>{d.toLocaleDateString('en',{weekday:'short'})}</small><b>{d.getDate()}</b></div>)}</div><div className="calendar-body">{days.map(d=>{const key=d.toISOString().slice(0,10);const rs=data.filter(r=>r.start_time.slice(0,10)===key);return <div className="day" key={key}>{rs.map(r=><div className="event" key={r.id}><b>{new Date(r.start_time).toLocaleTimeString('en',{hour:'numeric',minute:'2-digit'})}</b><span>{r.title}</span><small>{r.rooms.name}</small></div>)}</div>})}</div></div></>}
