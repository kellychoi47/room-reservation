export default function RoomsTab({rooms,onOpen,onRemove}){return <section className="card"><div className="section-head"><div><h3>Rooms</h3><p>Keep availability and location details accurate.</p></div><button className="primary" onClick={()=>onOpen('room')}>+ Add room</button></div><div className="table-wrap"><table><thead><tr>{['Room','Location','Capacity','Status',''].map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rooms.map(room=><tr key={room.id}><td>{room.name}</td><td>{`${room.building_name||room.location} · ${room.floor||''}`}</td><td>{room.capacity} people</td><td>{room.is_active?'Available':'Inactive'}</td><td><span className="row-actions"><button onClick={()=>onOpen('room',room)}>Edit</button>{room.is_active&&<button className="text-danger" onClick={()=>onRemove('room',room)}>Deactivate</button>}</span></td></tr>)}</tbody></table></div></section>}
