import { Edit3, Trash2, Upload } from 'lucide-react';
export default function FloorPlansTab({plans,onOpen,onDelete}){return <section className="floorplans-card"><div className="section-head"><div><h3>Floor plans</h3><p>Upload plans and assign rooms to map locations.</p></div><button className="primary" onClick={()=>onOpen('plan')}>+ Upload Floor Plan</button></div><div className="floorplan-grid">{plans.map(plan=><article className="floorplan-card" key={plan.id}><div className="floorplan-preview"><img src={plan.image_url} alt={`${plan.name} preview`}/></div><div className="floorplan-body"><div><h3>{plan.name}</h3><p>{plan.building_name} · {plan.floor}</p></div><span className="room-count">{plan.rooms?.length||0} rooms</span></div><footer><button onClick={()=>onOpen('plan',plan)}><Edit3 size={15}/>Edit</button><button onClick={()=>onOpen('plan',plan)}><Upload size={15}/>Upload new version</button><button className="danger" onClick={()=>onDelete(plan)}><Trash2 size={15}/>Delete</button></footer></article>)}{!plans.length&&<div className="empty">No floor plans yet. Upload your first plan to get started.</div>}</div></section>}
