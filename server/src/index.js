import 'dotenv/config';import express from 'express';import cors from 'cors';import helmet from 'helmet';import morgan from 'morgan';import rooms from './routes/rooms.js';import reservations from './routes/reservations.js';import dashboard from './routes/dashboard.js';
const app=express();app.use(helmet());app.use(cors({origin:process.env.CLIENT_ORIGIN?.split(',')||'http://localhost:5173'}));app.use(express.json({limit:'100kb'}));app.use(morgan('combined'));app.get('/health',(_q,res)=>res.json({ok:true}));app.use('/api/rooms',rooms);app.use('/api/reservations',reservations);app.use('/api',dashboard);app.use((err,_req,res,_next)=>{console.error(err);res.status(500).json({error:'An unexpected server error occurred.'})});app.listen(process.env.PORT||4000,()=>console.log('ReserveFlow API ready'));
