import 'dotenv/config';import express from 'express';import cors from 'cors';import helmet from 'helmet';import morgan from 'morgan';import rooms from './routes/rooms.js';import reservations from './routes/reservations.js';import dashboard from './routes/dashboard.js';import users from './routes/users.js';import floorPlans from './routes/floorPlans.js';
const app=express();app.use(helmet());app.use(cors({origin:process.env.CLIENT_ORIGIN?.split(',')||'http://localhost:5173'}));app.use(express.json({limit:'7mb'}));app.use(morgan('combined'));const health=(_q,res)=>res.json({ok:true,service:'reserveflow-api'});app.get('/health',health);app.get('/api/health',health);app.use('/api/rooms',rooms);app.use('/api/reservations',reservations);app.use('/api/users',users);app.use('/api/floor-plans',floorPlans);app.use('/api',dashboard);app.use((err,_req,res,_next)=>{console.error(err);res.status(500).json({error:err.message||'An unexpected server error occurred.'})});app.listen(process.env.PORT||4000,()=>console.log('ReserveFlow API ready'));
